# Autoply dinners viewer (SvelteKit)

This is a super-simple SvelteKit app that displays the data in the repo root `dinners.json`.

## Run

From the repo root:

- Dev server: `npm --prefix web run dev`
- Production preview: `npm --prefix web run preview`

If you edit `dinners.json`, refresh the page.
